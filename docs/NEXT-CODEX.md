# NEXT — 코덱스가 이어서 할 일

`docs/AUDIT-2026-08-22.md`(코덱스 감사) 후속 작업의 완료 상태를 기록한다.
아래 완료 항목은 다시 구현하거나 이전 화면으로 되돌리지 않는다.

**시작 전 `docs/WORLD.md` → `AGENTS.md` → `docs/HANDOVER.md` → `docs/CURRENT.md`의
LOCKED 표를 읽는다.**
아래 과제는 전부 그 규칙 안에서만 푼다.

지금은 **고양이 집사만** 손본다(CAT-FIRST). 다른 캐릭터 코드·대사는 건드리지 않는다.

## 2026-08-31 심사 후속 — 점수 올리기 라운드 (같은 브랜치)

P0·P1을 정리한 뒤, 심사에서 「8점대→9점대 레버」로 꼽았던 것과 반복 내성을 손봤다.

| 항목 | 내용 |
|---|---|
| **일기가 어제를 기억한다** | `diaryReflection`에 전날을 잇는 문장 추가(`catDiaryBridge`). 「어제 많이 힘들다고 했는데, 오늘은 청소까지 했다냥」. **바로 전날에 기록이 있을 때만** 잇는다 — 공백을 건너뛰면 기억이 아니라 출석 감시다. 어제도 힘든 날은 「오늘은 아무것도 안 해도 된다」로 받고, 어제 좋았던 날은 잇지 않는다 |
| 소견 풀 | 단계당 4줄 → **8줄**. 매일 쓰면 한 단계에 한동안 머무는데 4줄은 한 주면 바닥난다 |
| 완료 표현 | 「산책하고 왔어」가 대업으로 안 올라갔다. 절 나누기가 "-고"에서 잘라 행동과 시제가 갈렸다 → 뒤 절이 「왔/잤/먹었」로 시작할 때만 완료를 빌려준다 |
| 안 한 일 가드 | 「나가려다 말았어」의 "말았"이 과거형이라 안 나간 산책이 대업이 되고 있었다(이 라운드 이전부터) → `ABANDONED_MARK` |

### 여기서 배운 것 (같은 함정 두 번 밟지 말 것)

- **완료 판정을 넓힐 때는 반드시 「하려고 했는데」·「하고 싶었어」·「하려다 말았어」를
  같이 돌려본다.** 한국어 과거형 어미(ㅆ)는 의도·미완·포기에도 그대로 붙는다.
  넓히는 규칙은 허용 목록으로 짠다 — 막는 목록으로 짜면 반드시 샌다.
- **어제를 언급하는 기능은 「어제 기록이 있을 때만」이 조건이다.** 공백을 건너뛰어
  이으면 그 순간 감시가 된다.

### 3주차 정체 — 파일 권(卷)으로 해소 (오너 승인, 2026-08-31 구현)

방향이 정해졌다: **파일 「제2권」 — 축적 자체를 단위로 만든다.** 설계 근거와
규칙은 `docs/WORLD.md` §6 「파일은 차면 제본된다」에 있다. 요약:

| 규칙 | 내용 |
|---|---|
| 경계 | 유효 대업 50건을 채운 **날**에 권이 닫힌다 (`completedFileVolumes`, 전부 계산 — 저장 안 함) |
| 표시 | 겉장 탭 「주인님 파일 제N권」(data-volume-label) + 권 경계에 제본 확인서 |
| 의식 | 제본된 접수 직후, 방으로 돌아온 순간 한마디(권마다 1회, `fileVolumesNoticed`) |
| 금지 | 남은 건수 카운트다운 없음 — 재촉이 아니라 뒤늦은 인정 |

fixture 08(`cat-file-volumes.html`)이 이 계약을 지킨다. 사무국 행사(감사 시즌
등)는 다음 후보로 남아 있다 — 시즌 상품 구조와 이어진다.

---

## 2026-08-31 출시 심사 수리 (브랜치 claude/cat-rc-fix-2026-08-31)

`codex/cat-core-loop-simplify`(1c2ef75) 위에 얹었다. 출시 직전 전체 심사(84/100)에서
나온 P0·P1·P2를 고쳤다. 아래는 끝난 것이다 — 되돌리지 말 것.

| 등급 | 문제 | 수리 |
|---|---|---|
| P0 | 일정이 **딱 1건**이면 홈에서 목록도 완료 도장도 손잡이도 전부 사라졌다(2건부터는 정상) | 옛 `:only-child` 토글 숨김 규칙 삭제. 목록을 펴는 쪽은 접수 입력칸이 접히는 선 아래로 내려가 안 썼다(360px 694→772px) |
| P1 | 「발표했는데 완전 망했어」에 결과서가 뜨고 칭찬 도장이 찍혔다 | 결과가 나빴다는 신호를 감정 표에 추가 → 위로 우선, 결과서 없음. 한 일은 그대로 기록. 부기도 「해냈구냥」 대신 결과와 수행을 분리하는 줄로 |
| P1 | 칭호가 분야와 어긋난 채 파일에 영구 보존(발표 → 「답장을 보낸 전설」) | 저장 `category` 값은 그대로 두고 칭호 선택만 세분화(`NICKNAME_REFINEMENTS`) |
| P1 | 결과서를 닫고 온 접수대에 **직전 이야기 대사**가 남아 있었다 | `catHomePendingReaction`에 이 건의 마무리 한 줄 예약 |
| P2 | 집사에게 던진 질문에 경청 문구만 나갔다(「너는 뭐 좋아해?」 → 고백 답장) | 물음표 + 집사 호칭이면 인텐트 분기보다 먼저 받아 취향·이름·나이·안부에 답한다 |
| P2 | 첫 주 사용자가 선물을 줘도 방이 그대로(최저가 장난감 55P) | 깃털 낚싯대(30P)를 책상 장난감 후보에 추가 + 선반에 「책상에 놓임」 표시 |
| P2 | 기록 30건에서 파일 세로 5,766px | 지난 날짜 서류 접기 → 3,061px. **집사 일기는 접지 않는다** |
| P2 | 9건짜리 파일에 「최다 대업 씻기 · 1건」 | 같은 분야 2건부터 집계 |

### 일부러 안 한 것 (다시 제안하지 말 것)

- **고양이 홈의 「집사 응답」 카드 제거** — 죽은 DOM처럼 보이지만 다른 캐릭터의
  홈에서 실제로 쓰는 카드다. 고양이일 때만 숨는 것이 맞고, 지우면 다른 캐릭터가
  깨진다. CAT-FIRST는 "다른 캐릭터를 부수지 않는다"까지 포함한다.
- **결과서(FORM 05) 하단 3줄 접기** — 정보가 많은 건 맞지만 이 앱의 서명 같은
  화면이고 지금 잘 돌아간다. 릴리스 후보 계약도 핵심 화면 재설계를 막고 있다.
  손대려면 오너 승인을 먼저 받는다.

### 새로 생긴 규칙

- **대사에 실패 신호를 더할 때는 `message-interpreter.js`의 EMOTIONS 표에 넣는다.**
  거기 들어가야 위로 우선 경로와 결과서 억제가 같이 걸린다. chat-engine에서만
  분기하면 결과서는 그대로 뜬다.
- **`fillContentTemplate`의 조사 교정은 자리표시자에 바로 붙은 글자만 본다.**
  「{deed}」처럼 부호를 사이에 끼우면 조사가 안 맞는다 — 부호 뒤에는 조사를 두지 않는다.

검증: 회귀 **7/7 fixtures · 139 checks**, node 스위트 2종, 360/390/430 가로 넘침 0,
로컬 404 0, pageerror 0.

---

## ▶ 지금 상태와 다음 차례 (2026-08-24 기준)

**최신 작업선은 `claude/qa-fix-round1`이다.** `codex/cat-world-contract`(2cd3159) 위에
9커밋이 얹혀 있고, main에는 아직 안 들어갔다. 이어서 작업하려면 이 브랜치에서
새 브랜치를 판다. **main으로 merge하지 않는다 — 오너 승인 대기 중이다.**

```
git fetch origin claude/qa-fix-round1
git checkout -b <새-브랜치> origin/claude/qa-fix-round1
```

되돌릴 여지를 남기려고 코덱스 작업선은 한 줄도 고치지 않았다. 문제가 생기면
2cd3159로 돌아가면 된다.

### 이번에 새로 생긴 규칙 두 개

1. **끝내기 전에 `tests/cat-regression.html`을 브라우저로 연다.** 현재 7 fixtures ·
   107 checks. node 스위트 둘만 돌리고 넘기지 않는다 — 그래서 결손이 샜다.
   fixture를 새로 만들면 그 파일의 `CASES` 배열에도 올린다.
2. **대사에 이름·제목을 끼워 넣을 때는 `fillContentTemplate`을 거친다.**
   `{title}` 자리표시자를 쓰고 함수에 값을 넘기면 앞말 받침에 맞는 조사가 붙는다.
   직접 문자열을 이어붙이면 「병원 다녀오기**은** 어땠냥?」이 나간다.

### 아직 안 본 영역 (다음 평가 후보)

두 라운드에서 실측한 곳은 **홈·브리핑·접수대 방·인사 명부**다. 아래는 이번에
손대지 않았고 최근 실측 기록도 없다 — 무엇을 할지 지시가 따로 없으면 여기부터
신입 리뷰어 시선으로 훑고 보고한다.

| 영역 | 왜 후보인가 |
|---|---|
| 파일 탭 (기록·인증서·주간 보고서) | 두 라운드 모두 통과만 하고 화면을 재보지 않았다 |
| 채팅 흐름 | 브리핑 대사는 풀을 늘렸지만 채팅 응답 쪽은 그대로다. 같은 「매일 같은 말」 문제가 남아 있는지 확인 |
| 선물 데스크·희귀 반응 | 사운드만 바꿨고 화면·대사는 안 봤다 |
| 온보딩 첫 5분 | 복구 링크만 넣었다. 처음 켠 사람 시점으로 끝까지 통과해본 기록이 없다 |

### 오너 확인이 필요한 것 (에이전트가 정하지 않는다)

- 새 사운드 3종의 크기가 적당한지 (실기기에서만 판단 가능 · `sampleLevels`로 조정)
- iOS 사운드 토글 후 첫 재생, 햅틱, 공유 시트 — 전부 실기기 전용
- main merge 시점

---

## 2026-08-24 Claude QA-2 라운드 (브랜치 claude/qa-fix-round1)

오너가 UX·세계관·대사·편의성을 한 번에 올려달라고 해서 다섯 갈래를 묶어 손봤다.
아래는 전부 끝난 것이다 — 다시 구현하거나 되돌리지 말 것.

| 갈래 | 내용 |
|---|---|
| 팬 힌트가 명패를 가림 | 「좌우로 밀어 방 둘러보기」가 책상 위 명패 그림을 정통으로 덮고 있었다. 창구 명패 아래(창틀 위 벽)로 올렸다 — 그 자리는 가릴 소품이 없다 |
| 접수 입력칸이 화면 밖 | 첫 화면 기준 입력칸이 801px, 하단 네비가 772px이라 통째로 가려 있었다. 방·빈 브리핑·머리글에서 걷어내 735px로 올렸고, 홈 머리글을 누르면 입력칸으로 스크롤+포커스한다. 고양이 홈에는 이 안내가 아예 없었다 — `#first-deed-guide`는 집사 응답 카드 안에 있는데 그 카드는 방이 대신하느라 숨겨진다 |
| 브리핑 대사 단벌 | 등록·질문·완료가 단계당 한 줄씩이라 매일 쓰면 같은 말이 나왔다. 단계(early/mid/late)×3갈래로 늘리고 직전 줄은 건너뛴다. 시간 미정 첫 문장도 3종 |
| 저녁 마무리 | 저녁 대사 3종 고정 → 상황별(처리 0건/일부/전부) 3종씩. 저녁에 **마지막 남은 건**을 끝내면 평범한 완료 멘트 대신 하루를 닫는 줄이 나간다. 처리 0건에 재촉하는 말은 넣지 않는다 |
| 조사 오류 | 「병원 다녀오기**은** 어땠냥?」. 새 대사는 전부 `fillContentTemplate`을 타게 해서 받침에 맞춘다 — 대사를 새로 넣을 때도 `{title}` 자리표시자를 쓰고 이 함수를 거쳐라 |
| 완료 반응이 서류에 안 남음 | `.briefing-item-reaction`은 CSS와 fixture에 있는데 **그리는 코드가 없었다**. 완료 반응이 슬립에 3.6초 떴다 사라질 뿐이었다. 렌더를 복원했다 |
| 죽어 있던 fixture | `tests/briefing-story-flow.html`이 회귀 하네스 목록에 없어 아무도 안 돌렸고, 위 결손 때문에 실패 상태였다. 하네스에 07번으로 등록했다 |
| 하네스 타임아웃 | 25 checks짜리 fixture가 9초를 써서 10초 제한에 걸려 떨어졌다(통과할 검사인데도). 20초로 올렸다 |

검증: 회귀 **7/7 fixtures · 107 checks**, node 스위트 2종, 3폭(360/390/430) 실측, pageerror 0.

R4(전임 집사 대기 발령·사내 사건)는 이번에 처음 실측했고 **정상**이다. 사건 기록이
뜨고, 3폭에서 넘치지 않고, 「퇴사」가 아니라 「대기 발령 · 복귀 가능」 어휘를 지킨다.

## 2026-08-24 Claude QA-1 라운드 (브랜치 claude/qa-fix-round1)

코덱스 작업선 전체 품질 평가(총점 78) 후 P1 4건을 수리했다. 다시 손대지 말 것:

| 수리 | 내용 |
|---|---|
| 부서명 통일 | 「첫기록과」 6곳 → **「기록1과」** (오너 확정). 이걸로 자체 회귀 검사가 6/6 fixtures · 99 checks 전체 통과가 됐다. 다른 캐릭터는 나중에 기록2과·3과를 받는다(WORLD.md §4) |
| 결과서 넘침 | 브리핑 배너 max-content가 종이를 400px+로 부풀려 오른쪽 변이 잘렸다 → modal min-width:0 + 배너 fit-content/ellipsis |
| 방 아이템 이중음 | cueGeneralControl 제외 목록에 #cat-room-gifts 추가 — 물건 탭 소리는 슬립 종이음 하나 |
| 시간 미정 슬립 | 「시간은 아직 안 정해졌다냥에 병원 있다냥」 문장 조립 → 시간 유무로 분기 |
| 온보딩 복구 진입점 | 발령서 아래 「보관본이 있어요 · 복구하기」 — 기기 교체 시 임시 발령 없이 복원 |

### 사운드 — final-v3 확정 (오너 직접 선택)

`design/tools/sound-preview-claude.html`로 A/B를 듣고 오너가 고른 결과다.
**임의로 되돌리거나 다른 소리로 바꾸지 않는다.**

| 슬롯 | 확정 | 비고 |
|---|---|---|
| 버튼·화면 이동 | `ui-wood-tick` | 나무 책상을 손끝으로 톡 |
| 일정 처리 완료 | `stamp-tiny-bounce` | 도장 찍고 살짝 튕기는 손맛 |
| 선물 전달 | `gift-pop` | 위로 미끄러지는 톤 하나 |
| 관계 해금 | `relationship-unlock` (v2 유지) | 오너가 현재 소리를 선택 |
| 고양이 3종 | v2 유지 | 실제 녹음이라 합성으로 대체하지 않았다 |

`design/sound-assets/final-v3/`에 일곱 개가 다 들어 있고 app.js는 이 폴더만
본다. v2 폴더는 그대로 남겨뒀으니 되돌릴 일이 생기면 경로와 `sampleLevels`만
원복하면 된다(app.js 주석에 원복값을 적어뒀다).

새 합성물이 v2보다 피크가 4~7dB 높아 `sampleLevels`에서 ui/stampTiny/gift를
낮췄다. **소리 파일을 새로 갈면 레벨도 같이 재본다** — 안 그러면 갈아끼운
소리만 튄다.

## 2026-08-22 Codex 작업선 완료 현황

| 항목 | 상태 | 커밋 |
|---|---|---|
| A-1 홈 CTA 첫 화면 안으로 복구 | 완료 | `0b6bb79` |
| B-2 옛 기념 증서와 공식 인정 계보 분리 | 완료 | `5226d04` |
| A-2 브라우저 fixture assertion 추가 | 완료 | `920ad78` |
| R4 전임 고양이 집사 대기 발령과 사내 사건 | 완료 | `bc28730` |
| CAT 등급 표시 라벨 정리 | 완료 | `9e6a4d7` |
| legacy 기념 증서 열람 경로 복구 | 완료 | `58d0361` |
| CAT 통합 브라우저 회귀 검사 | 완료 | `529c6f6`, `e026a15` |

현재 문서에 남은 다른 캐릭터 세계관 작업은 방향만 확정된 상태다.
**CAT-FIRST 해제에 대한 오너의 명시적 지시 전에는 시작하지 않는다.**

---

## 수리 완료 (다시 손대지 말 것)

| | 무엇 | 어떻게 |
|---|---|---|
| B-1 | 접속 시간 추론 대사 | 「올 시간은 알고 있었다냥」 → 「결재판은 비워뒀다냥. 규정이다냥.」 |
| B-3 | 기록 0건 계정에 기록 습관 단정 | 「{owner}이 자주 적는 칸은」 → 「{owner} 파일은 손 닿는 데 둔다냥.」 |
| S-1 | 파일 안내문 ↔ 중복 접수 규칙 불일치 | 보존과 도장 자격을 분리해 다시 씀 |
| S-2 | 「누적 명성 6건」 | 「건」 제거 |
| A-3 | 「열어보고 싶으면 대업을 모으세요」 | 「개봉 시점은 사무국이 정합니다」 |

---

## 완료 기록 1 — A-1 홈 CTA가 첫 화면 밖이다

감사에서는 `아쉬움`·360px 문제로 분류됐지만, 재측정 결과 **세 폭 전부**였다.
이 앱의 핵심 행동이 스크롤 없이는 닿지 않는다.

기록 없는 고양이 계정을 최상단으로 열었을 때 (viewport 높이 800):

| 폭 | 입력창 | 접수버튼 | 하단 nav 상단 |
|---|---|---|---|
| 360px | 670~771 | **861~930** | 728 |
| 390px | 652~753 | **844~913** | 728 |
| 430px | 652~753 | **844~913** | 728 |

접수 버튼은 세 폭 모두 화면 밖이고, **입력창도 아래 절반이 고정 nav에 가려진다.**

### 제약

HOME 구조·집사방·어제 일기 통지는 **LOCKED**다. 없애거나 순서를 바꾸는 방향은 안 된다.
방 높이 축소, 간격 조정, 입력 포커스 시 스크롤 보조처럼 LOCKED 안쪽 수단으로 푼다.

### 절차

**코드를 고치기 전에 2~3안을 제안하고 승인을 받는다.** 각 안마다:

- 무엇을 얼마나 바꾸는지 (구체적 수치)
- 세 폭에서의 예상 좌표 (입력창 / 접수버튼 / nav)
- 잃는 것 — 특히 집사방이 작아지면 방에 놓인 물건이 얼마나 작아지는지
- LOCKED 표의 어떤 행과 닿는지

---

## 완료 기록 2 — B-2 legacy 인증서 모순

legacy `achievements`의 `isCertificate !== false` 항목이 전부 `state.certificates`로
이관되는데, 진행도는 `officialRecords().length`로 따로 계산한다. 그래서 파일 화면에
**「공식 인정 4건」과 「5건이 모이면 발급합니다 (1건 남음)」이 동시에** 뜬다.

### 먼저 오너가 답해야 하는 것

> 예전 버전에서 받은 기념 증서를, 지금의 "공식 인정"으로 쳐줄 것인가?
>
> - **(가) 쳐준다** — 옛 4장이 공식 인정으로 남고 진행도 계산도 그것을 반영한다. 옛 사용자를 존중하는 쪽.
> - **(나) 별개로 둔다** — 옛 증서는 보관하되 공식 인정 계보에는 넣지 않는다. 계산이 깨끗한 쪽.
>
> 실제 legacy 사용자가 거의 없다면 (나)를 권한다.

**오너 결정: (나) — 옛 기념 증서는 공식 인정 계보에서 분리한다 (2026-08-22 확정).**

즉 공식 인정은 새 규칙(첫 5건, 이후 7건 주기)으로만 센다. 옛 기념 증서는
삭제하지 않고 보관하되, 진행도 계산과 발급 회차에는 넣지 않는다.
「공식 인정 4건」과 「1건 남음」이 동시에 뜨던 모순은 이 기준으로 없앤다.

### 수리 조건

- **기존 저장 데이터를 절대 삭제하지 않는다.** 어떤 결정이든 옛 증서는 보관된다.
- `butlermaker_v1` 저장 계약에 새 키를 늘리지 않는다. 파생으로 되면 파생으로 한다.
- 인증서 회차 숫자(`certificateTargetFor`)는 과거 증서를 다시 열었을 때도 그때의
  목표를 말해야 한다 — LOCKED 「인증서 숫자」 행.
- `tests/personnel-migration.html` fixture로 실제 재현하고, 수리 후 같은 fixture에서
  두 표시가 일치하는지 확인해 보고한다.

---

## 완료 기록 3 — A-2 마이그레이션 테스트가 검증을 하지 않는다

`tests/legacy-migration.html`, `tests/diary-reveal.html`, `tests/personnel-migration.html`
셋 다 localStorage만 심고 앱으로 이동한다. **B-2 모순이 화면에 그대로 나타났는데도
테스트 실패가 되지 않았다.**

fixture 역할은 유지하되 핵심 계약에 assertion을 붙인다:

- 이관된 인증서 수와 진행도 안내가 서로 모순되지 않는가
- legacy source fallback (`records`가 없으면 `achievements` 사용)이 동작하는가
- 같은 날짜에 legacy 공개 일기와 신규 봉인 일기가 섞였을 때 공개가 우선인가

기존 두 스위트(`node tests/*.test.js`)처럼 CLI에서 돌릴 수 있으면 그게 낫다.
브라우저가 꼭 필요하면 그 이유를 적는다.

---

## 완료 및 보류 기록 4 — 그 뒤

| | 내용 | 비고 |
|---|---|---|
| R4 | 캐릭터 교체 어휘를 "대기 발령"으로 + 전임 집사의 사내 사건 | **방향·톤은 이미 확정돼 있다 — `docs/WORLD.md` §3-1을 그대로 따른다.** 저장·복원 로직은 이미 있고 「대기 발령」 어휘만 없다. 전임은 **불쌍함이 아니라 질투·참견**으로 그린다(처량하면 죄책감이 된다). 사내 사건 게시판 surface는 새로 만들어야 하므로 화면 추가 제안 먼저. 이건 캐릭터 판매의 전제 조건이다 |
| — | `grade` 라벨 정리(`인류사적 대업`·`우주 최초 기록`) | 기록에 저장되는 공용 값이라 표시만 바꾸는 방법을 먼저 제안 |
| — | 다른 캐릭터에 각자의 세계관을 얹는다 | **말투만 고치는 작업이 아니다.** `docs/WORLD.md` §4의 질문 넷(왜 하필 나인가 / 무엇을 핑계로 삼는가 / 무엇이 쌓이는가 / 무엇을 말하지 않는가)에 캐릭터마다 답을 정하고, 그 답 위에서 대사를 다시 쓴다. **B-3과 같은 결함도 여기서 같이 잡는다** — AI 관계 문장 「[선제 준비] {owner} 자주 쓰는 양식」이 기록 0건에도 나간다 |

---

## 공통 규칙

- 끝내기 전: `node --check app.js` → 두 테스트 스위트 → **360/390/430 세 폭에서 실제로 열어 확인**.
- 레이아웃·겹침·이벤트는 눈으로 보지 말고 **숫자로 잰다**(`docs/HANDOVER.md` §7).
- 커밋은 항목별로 나눈다. **브랜치를 새로 파서 올리고 main은 건드리지 않는다.**
- LOCKED와 부딪히는 판단이 필요하면 임의로 정하지 말고 **먼저 지적하고 승인을 받는다.**
- 오너가 파일을 첨부하지 않아도 되도록, 결과는 저장소 안 문서로 남긴다.
